# Docs: https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: app-svc
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80         # ClusterIP port
      targetPort: http # Frontend container port
  selector:
    app: frontend