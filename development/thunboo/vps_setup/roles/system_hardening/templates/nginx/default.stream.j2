map $ssl_preread_server_name $sni_name {
    hostnames;

    include /etc/nginx/stream-maps/sni/*.conf;
}

server {
    listen          443;
    ssl_preread     on;
    proxy_pass      $sni_name;

    #proxy_protocol on;
    set_real_ip_from unix:;
}